{
  "name": "my-life-portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "export": "next build && mkdir -p docs && cp -r out/* docs/ && echo '' > docs/.nojekyll",
    "export-local": "cp next.config.local.js next.config.js && next build && mkdir -p test-build && cp -r out/* test-build/ && git checkout next.config.js"
  }
}
